
@model IEnumerable<LinkViewModel>
@{
    string strTitle = "Simplify your link";
    ViewBag.Title = strTitle;
}

<div>
    <h2>@strTitle</h2>
    <div>
        <div class="div-box">
            @*<a asp-action="AddLinkForm">Add Link</a>*@


            <form method="post" action="AddLink">
                <div class="row">
                    <div class="col-md-10">
                        <input type="text" name="message" class="form-control" placeholder="">
                    </div>
                    <div class="col-md-2">
                        <button type="submit" class="btn btn-primary btn-block">Send</button>
                    </div>
                </div>
            </form>

        </div>
@*        @if (Model.Any())
        {
*@
            <div class="table-responsive">
                <table class="table table-striped table-hover table-bordered">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(Model=>Model.Url)
                            </th>
                            <th>
                                @Html.DisplayNameFor(Model=>Model.DateAdded)
                            </th>
                            <th>
                                @Html.DisplayNameFor(Model=>Model.MinUrl)
                            </th>
                            <th>
                                @Html.DisplayNameFor(Model=>Model.Ticket)
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var emp in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem=>emp.Url)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem=>emp.DateAdded)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem=>emp.MinUrl)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem=>emp.Ticket)
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
@*

        }*@
    </div>

    <script src="https://www.gstatic.com/firebasejs/8.2.1/firebase-app.js"></script>
    <script src="https://www.gstatic.com/firebasejs/8.2.1/firebase-dynamic-links.js"></script>
    <script type="module">
        // Import the functions you need from the SDKs you need
        import { initializeApp } from "https://www.gstatic.com/firebasejs/9.22.1/firebase-app.js";
        import { getAnalytics } from "https://www.gstatic.com/firebasejs/9.22.1/firebase-analytics.js";
        // TODO: Add SDKs for Firebase products that you want to use
        // https://firebase.google.com/docs/web/setup#available-libraries

        // Your web app's Firebase configuration
        // For Firebase JS SDK v7.20.0 and later, measurementId is optional
        const firebaseConfig = {
            apiKey: "AIzaSyCy_vearaUXyRu2-7lNeGGnhRALxY7bLqc",
            authDomain: "primeval-argon-388710.firebaseapp.com",
            projectId: "primeval-argon-388710",
            storageBucket: "primeval-argon-388710.appspot.com",
            messagingSenderId: "1073623906680",
            appId: "1:1073623906680:web:7ad564af15c103a77c308d",
            measurementId: "G-FVWQ7YRFPJ"
        };

        // Initialize Firebase
        const app = initializeApp(firebaseConfig);
        const analytics = getAnalytics(app);

    </script>
    



